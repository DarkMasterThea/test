export type Card = {
  cardNumber: number,
  cvv: number,
  expirationMonth: string,
  expirationYear: string,
  email: string
}
export type CardRs = {
  cardNumber: number,
  expirationMonth: string,
  expirationYear: string,
  email: string
}
